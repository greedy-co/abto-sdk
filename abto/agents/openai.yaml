interface:
  display_name: "ABTO SDK"
  short_description: "Map repos, wire LLM calls, and select events"
  default_prompt: "Use $abto to map the frontend and backend repositories, inventory LLM call sites for approval, wire the selected ABTO core, and let me choose events."
